const UnIdentifiedNav = () => {
  return (
    <div className="w-full h-full flex justify-center items-center text-lg text-red-500">
      Nav Route provided doesn't have component or does not exist
    </div>
  );
};

export default UnIdentifiedNav;
