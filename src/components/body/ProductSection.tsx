import CategorySelector from "./ProductSection/CategorySelector";
import ProductList from "./ProductSection/ProductList";

const ProductSection = () => {
  return (
    <section>
      <CategorySelector />
      <ProductList />
    </section>
  );
};

export default ProductSection;
